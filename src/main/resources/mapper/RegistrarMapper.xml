<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hos.mapper.RegisterMapper">
    <select id="queryRegistrarById" resultType="com.hos.entity.Registrar">
          select * from registrar where register_id=#{params.register_id}
    </select>

</mapper>